repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run pytest
        entry: python -m pytest tests/ -x -q
        language: system
        pass_filenames: false
        always_run: true
