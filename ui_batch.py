"""Tab 2 · Make a batch: generate formulations, make them, record results."""
import streamlit as st


def render(opt, storage):
    st.subheader("Make a batch")
    st.caption("Generating and recording move here in the next task.")
